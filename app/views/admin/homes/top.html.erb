<div class="container">
  <div class="row">
   <div class="mt-3 mb-3">
  <headline>注文履歴一覧</headline>
  </div>
</div>
<table class="table" align="center">
  <thead>
    <tr bgcolor="#f5f5f5">
    <th>購入日時</th><th>購入者</th>
    <th>注文個数</th><th>注文ステータス</th>
    </tr>
  </thead>
</table>
<tbody>
<% @order.each do |order| %>
<td><%= link_to l order.created_at admin_orders_path(order) %></td>
<td><%= order.full_name %></td>
<td><%= order_detail.amount %></td>
<td><%= order.stutas %></td
<td>
  <% if @customer.is_deleted %>
  <p>有効</p>
  <% else %>
  <p>退会</p>
  <% end %>
</td>
</tr>
<% end %>
</tbody>
</table>
  </div>

