<div class="container">
 <div class="row">
  <div class="mt-3 mb-3">
   <headline><%= @customer.full_name %>さんの会員詳細</headline>
   </div>
  </div>
 </div>
<div class="container">
 <div class="row">
  <%= form_with model: @customers do |f| %>
   <table class="table table-borderless">
    <tr><td>会員id</td>
     <td><%= @customer.id %></td></tr>
    <tr><td>氏名</td>
  <td><%= @customer.full_name %></td></tr>
 <tr><td>フリガナ</td>
<td><%= @customer.full_name_kana %></td></tr>
 <tr><td>郵便番号</td>
   <td><%= @customer.postal_code %></td></tr>
     <tr><td>住所</td>
   <td><%= @customer.address %></td></tr>
 <tr><td>電話番号</td>
<td><%= @customer.telephon_number%></td></tr>
 <tr><td>メールアドレス</td>
  <td><%= @customer.email %></td></tr>
   <tr><td>会員ステータス</td>
   <td>
 <% if @customer.is_deleted %>
  退会
<% else %>
  有効
 <% end %>
  <% end %>
  </td>
</tr>
</table>
<%= link_to "編集する", edit_admin_customer_path(@customer), class: "btn btn-lg btn-success" %>

<%= link_to "注文履歴一覧を見る", edit_admin_customer_path(@customer), class: "btn btn-lg btn-primary" %>
  </div>
</div>


